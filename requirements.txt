# Core dependencies (governance engine + instances)
numpy>=1.21
pandas>=1.3

# API server
fastapi>=0.100
uvicorn>=0.20
pydantic>=2.0
httpx>=0.24       # required by FastAPI TestClient for testing

# Tests
pytest>=7.0
