[project]
name = "sensory-architecture-factory"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.21",
    "pandas>=1.3",
    "fastapi>=0.100",
    "uvicorn>=0.20",
    "pydantic>=2.0",
    "httpx>=0.24",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]
